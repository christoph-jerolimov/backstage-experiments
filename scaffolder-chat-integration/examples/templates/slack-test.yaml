# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-template
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: slack-test
  title: Slack Test
  description: An example template for the scaffolder that sends a slack message
spec:
  owner: user:guest
  type: service

  # These parameters are used to generate the input form in the frontend, and are
  # used to gather input data for the execution of the template.
  parameters:
    - title: Fill in some steps
      required:
        - message
      properties:
        message:
          title: Message
          type: string
          ui:autofocus: true

  # These steps are executed in the scaffolder backend, using data that we gathered
  # via the parameters above.
  steps:
    # Let's notify the user that the template has completed using the Notification action
    - id: message
      name: Message
      action: slack:sendMessage:conversation
      input:
        message: ${{ parameters.message }}
        conversationId: C09U9GA7MJ7

